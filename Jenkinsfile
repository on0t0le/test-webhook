node('slave1') {
  stage('Preparing') {       
    git 'https://github.com/on0t0le/test-webhook.git'
  }
  stage('Test stage') {
    echo 'Hello stage!'
  }
  stage('Test  pipeline') {
    echo 'New text!'
  }
}
